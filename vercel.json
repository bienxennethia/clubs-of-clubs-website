{
  "version": 2,
  "builds": [
    { "src": "/", "use": "@vercel/static-build" },
    { "src": "/client/package.json", "use": "@vercel/static-build", "config": { "buildCommand": "npm install && npm run build" }}
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
