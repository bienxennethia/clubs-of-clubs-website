{
  "version": 2,
  "builds": [
    { "src": "/client", "use": "@vercel/static-build", "config": { "outputDirectory": "/" }},
    { "src": "/server/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/$1" }
  ]
}
