{
  "version": 2,
  "builds": [
    { "src": "/client", "use": "@vercel/static-build", "config": { "distDir": "build" }},
    { "src": "/server/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/$1" },
    { "src": "/(.*)", "dest": "/client/build/$1" }
  ]
}
